{
  // Solution file: builds the library graph in dependency order via `tsc -b`.
  // apps/desktop is NOT referenced here: electron-vite owns its build, and its
  // type checking runs through `pnpm --filter aether-forge-desktop typecheck`.
  "files": [],
  "references": [
    { "path": "packages/core" },
    { "path": "packages/ui" },
    { "path": "packages/system-toy" },
    { "path": "packages/system-ironsworn" },
    { "path": "packages/importer-datasworn" }
  ]
}
